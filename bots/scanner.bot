name: 'scanner'
data: { tick: 0 }
work: ->
	@data.tick += 1
	angle = @rand(360)
	range = @scan(angle, 1)
	if range > 0
		@shoot(angle, range)
	if @rand(100) == 0 || @data.tick == 1
		randomHeading = @rand(360)
		randomSpeed = @rand(4) + 1
		@drive(randomHeading, randomSpeed)
		
	